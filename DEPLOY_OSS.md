
# 阿里云 OSS 静态网站托管 - 超简单部署指南！
## 小侠专属方案 ⚡

## 🚀 为什么用 OSS？
- **更简单**：只需要上传 `out/` 文件夹里的静态文件
- **更快速**：自带 CDN 加速，全球访问都快
- **更省钱**：OSS 存储非常便宜，甚至免费额度够用
- **零维护**：不需要运行服务器，上传即用

---

## 📋 部署步骤（4步搞定）

### 第1步：在本地构建项目（你已经做过了！）
```bash
cd ai-content-site
npm install
npm run build
```
✅ 确认 `out/` 文件夹已生成

---

### 第2步：创建 OSS Bucket

1. 打开阿里云 OSS 控制台：https://oss.console.aliyun.com/
2. 点击"创建 Bucket"
3. 按下面配置：

| 配置项 | 值 |
|--------|-----|
| **Bucket 名称** | `ai-content-site`（或者你喜欢的名字） |
| **地域** | 华东1（杭州） |
| **存储类型** | 标准存储 |
| **冗余类型** | 本地冗余存储（LRS） |
| **读写权限** | 公共读 |

4. 点击"确定"创建

---

### 第3步：开启静态网站托管

1. 进入刚创建的 Bucket 详情页
2. 点击左侧菜单"数据管理" -&gt; "静态页面"
3. 点击"设置"
4. 按下面配置：

| 配置项 | 值 |
|--------|-----|
| **默认首页** | `index.html` |
| **默认 404 页** | `404.html` |
| **子目录首页** | 启用 |
| **索引文档** | `index.html` |

5. 点击"确定"

---

### 第4步：上传文件

1. 点击左侧菜单"文件管理" -&gt; "文件列表"
2. 点击"上传文件"
3. **重要**：点击"上传文件夹"
4. 选择你本地的 `out/` 文件夹（注意：是 `out/` 文件夹，不是整个项目！）
5. 点击"上传"
6. 等待上传完成...

---

## ✅ 完成！获取访问地址

上传完成后：

1. 回到"静态页面"设置页
2. 你会看到** Bucket 域名**，类似这样：
   ```
   http://ai-content-site.oss-cn-hangzhou.aliyuncs.com
   ```
3. 点击这个地址就能看到你的网站了！

---

## 🌟 进阶：绑定自定义域名（可选）

如果你想用自己的域名访问：

1. 在 Bucket 详情页点击"域名管理"
2. 点击"绑定域名"
3. 输入你的域名（比如 `www.yourdomain.com`）
4. 在你的域名 DNS 服务商那里添加 CNAME 记录
5. 开启"CDN 加速"（推荐！）

---

## 📝 小提示

- **只上传 `out/` 文件夹**：不要上传 `node_modules`、`.git` 这些
- **public-read 权限**：必须设为公共读，不然别人访问不了
- **后续更新**：修改内容后，重新 `npm run build`，然后重新上传 `out/` 文件夹覆盖就行

---

## 🎉 就这么简单！

有问题随时叫我，小侠！🚀

