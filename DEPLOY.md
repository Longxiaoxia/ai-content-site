
# 阿里云FC 超级简单部署指南！
## 小侠专属方案

## 🚀 最快部署方法（3步搞定）

### 第1步：在你的Windows电脑上构建项目

在你的Windows电脑上打开终端，运行：

```bash
cd ai-content-site
npm install
npm run build
```

这会生成 `out/` 文件夹，里面就是网站的所有静态文件。

---

### 第2步：在阿里云FC创建函数

1. 打开阿里云函数计算控制台：https://fcnext.console.aliyun.com/
2. 点击"创建函数"
3. 选择"Web函数"
4. 按下面配置：

| 配置项 | 值 |
|--------|-----|
| **地域** | 华东1（杭州）cn-hangzhou |
| **函数名称** | `ai-content-server` |
| **运行环境** | Node.js 22 |
| **代码上传方式** | 在线编辑（先创建后上传文件夹） |
| **启动命令** | `npm run start` |
| **监听端口** | 9000 |

5. 点击"创建"

---

### 第3步：上传代码

函数创建成功后：

1. 进入函数详情页
2. 点击"函数代码"标签
3. 点击"上传" -&gt; "上传文件夹"
4. 选择你本地电脑的整个 `ai-content-site` 文件夹上传（包含：
   - `out/` 文件夹（构建好的静态文件）
   - `server.js`
   - `package.json`
   - 等等所有文件

5. 上传完成后，点击"部署代码"

---

## ✅ 完成！

部署成功后，你会看到一个访问地址，点击就能看到你的网站了！

---

## 📝 需要我帮你做什么？
