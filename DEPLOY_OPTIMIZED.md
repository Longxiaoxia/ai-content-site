
# 🚀 优化部署方案 - 代码包瘦身指南

## 问题分析
当前部署包太大是因为包含了 `node_modules` 文件夹，其实我们不需要它！

## ✅ 优化方案（推荐）

### 方案一：使用阿里云 OSS 静态网站托管（最简单，免费！）

这是最适合静态网站的方案！

#### 1. 开通 OSS
1. 打开 https://oss.console.aliyun.com/
2. 创建 Bucket
   - 地域：华东1（杭州）
   - 存储空间名称：`ai-content-site`（或你喜欢的名字）
   - 存储类型：标准存储
   - 读写权限：公共读

#### 2. 上传文件
1. 进入 Bucket
2. 点击"文件管理" -&gt; "上传文件"
3. 将 `out/` 文件夹里的**所有内容**上传（不要上传 `out` 文件夹本身，直接上传里面的文件）

#### 3. 开启静态网站托管
1. 点击"数据管理" -&gt; "静态页面"
2. 点击"设置"
3. 配置：
   - 默认首页：index.html
   - 默认 404 页：404.html
4. 保存

#### 4. 获得访问地址！
完成后你会得到一个访问地址，类似：
`http://ai-content-site.oss-cn-hangzhou.aliyuncs.com`

还可以绑定自己的域名！

---

### 方案二：优化 FC 部署（如果还想用 FC）

#### 1. 创建精简的部署包

在你的 Windows 电脑上，只需要上传这些文件：
```
ai-content-site/
├── server.js          ✓ 需要
├── package.json       ✓ 需要
└── out/               ✓ 需要（构建好的静态文件）
    ├── index.html
    ├── 404.html
    ├── _next/
    └── ...
```

**不要上传**：
- ❌ `node_modules/` （太大了！）
- ❌ `.git/`
- ❌ `.DS_Store`
- ❌ `src/` （源代码已经构建了）

#### 2. 在 FC 控制台重新上传

1. 进入函数详情
2. 点击"函数代码"
3. 删除现有的代码
4. 只上传上面列出的必需文件
5. 点击"部署代码"

---

## 🎯 我的建议

**小侠，我强烈推荐方案一（OSS 静态托管）**，因为：

| 对比项 | FC | OSS |
|--------|-----|-----|
| 费用 | 需要按调用付费 | 基本免费 |
| 速度 | 一般 | CDN 加速，飞快！|
| 维护 | 需要管理服务器 | 完全托管 |
| 简单度 | 需要写 server.js | 直接上传文件 |

---

## 💡 需要我帮你做什么？

1. 要不要我帮你准备 OSS 部署的详细步骤？
2. 还是你想继续用 FC，我帮你做精简打包？
3. 或者先看看现在的网站能不能正常访问？

快告诉我你的想法！🚀

